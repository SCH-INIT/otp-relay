{
  "private": true,
  "scripts": {
    "build": "esbuild app.jsx --bundle --minify --outfile=app.js --define:process.env.NODE_ENV='\"production\"'"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}

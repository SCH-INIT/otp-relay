diff --git a/k8s/Dockerfile b/k8s/Dockerfile
index 9dc68dd..8b65297 100644
--- a/k8s/Dockerfile
+++ b/k8s/Dockerfile
@@ -11,20 +11,13 @@ RUN npx babel frontend/app.jsx --presets @babel/preset-react --out-file frontend
 # Install Python dependencies and build help docs.
 FROM python:3.12-slim AS builder
 WORKDIR /build
+COPY requirements.txt .
+RUN python -m venv /build/venv && \
+    /build/venv/bin/pip install --no-cache-dir -r requirements.txt
 COPY main.py .
 COPY --from=frontend /build/frontend/ ./frontend/
 COPY docs/help/ ./docs/help/
 COPY scripts/build_help_docs.py ./scripts/build_help_docs.py
-RUN python -m venv /build/venv && \
-    /build/venv/bin/pip install --no-cache-dir \
-        fastapi \
-        uvicorn \
-        openpyxl \
-        python-dotenv \
-        bcrypt \
-        python-multipart \
-        markdown \
-        pyyaml
 # Build help docs: converts docs/help/*.md into frontend/help/wizard-guide.json
 # and frontend/help/rendered/*.html.
 RUN /build/venv/bin/python scripts/build_help_docs.py
diff --git a/k8s/Dockerfile.monitor b/k8s/Dockerfile.monitor
index 57fc119..e88a90c 100644
--- a/k8s/Dockerfile.monitor
+++ b/k8s/Dockerfile.monitor
@@ -18,10 +18,12 @@ RUN useradd --system --uid 999 --no-create-home --shell /usr/sbin/nologin otprel
 
 WORKDIR /app
 
-# Install Python dependencies
+# Install Python dependencies. Uses the same requirements.txt as the main app
+# image — the monitor over-installs a few packages (fastapi, etc.) it does not
+# use. Cost is small and keeps a single source of truth for dependencies.
+COPY requirements.txt .
 RUN python -m venv /app/venv && \
-    /app/venv/bin/pip install --no-cache-dir \
-        python-dotenv
+    /app/venv/bin/pip install --no-cache-dir -r requirements.txt
 
 COPY monitor.py .
 

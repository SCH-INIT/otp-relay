apiVersion: v1
kind: Service
metadata:
  name: otp-relay
  namespace: otp-relay
  labels:
    app: otp-relay
spec:
  type: ClusterIP
  selector:
    app: otp-relay
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
